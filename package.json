{
  "name": "circuit-breaker",
  "version": "1.0.0",
  "description": "simple circuit breaker middleware for express.js",
  "main": "index.js",
  "scripts": {
    "test": "mocha || test",
    "build": "NODE_ENV=dev && npx babel src -d build",
    "clean": "rm -rf build/",
    "start": "node build/circuit_breaker.js"
  },
  "author": "@bhargavpanth",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-register": "^6.26.0",
    "chai": "^4.1.2",
    "mocha": "^5.1.0"
  },
  "dependencies": {
    "node-fetch": "^2.1.2"
  }
}
